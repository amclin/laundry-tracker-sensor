{
  "name": "laundry-sensor",
  "version": "0.0.1",
  "description": "Client services to run on a Raspberry Pi to detect whether or not laundry machines are running.",
  "main": "index.js",
  "scripts": {
    "test": "tap test/*.js --cov",
    "pretest": "standard",
    "start": "node index.js",
    "deploy": "./deploy.sh"
  },
  "author": "Anthony McLin",
  "license": "MIT",
  "devDependencies": {
    "standard": "^12.0.1",
    "tap": "^13.1.2"
  },
  "dependencies": {
    "request": "^2.88.0",
    "rpio": "^1.0.13"
  }
}
